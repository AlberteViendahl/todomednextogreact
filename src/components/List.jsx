import ListItem from "./ListItem";

const List = ({ tasks }) => {
  return (
    <section className="">
      <div>
        <h1 className="font-semibold">ToDo</h1>
        {tasks && div}
      </div>
      <div>
        <h1 className="font-semibold">Done</h1>
      </div>
    </section>
  );
};

export default List;
